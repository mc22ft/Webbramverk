

<li>
  <%= link_to user.name, user %> <%= user.userapps.count %>st API nycklar
  <% if current_user.admin? && !current_user?(user) %>
    | <%= link_to 'radera', user, method: :delete,
                  data: { confirm: 'Är du säker?' } %>
<% end %>
</li>